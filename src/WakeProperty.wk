import WakeType;

every WakeProperty is:

	needs public WakeType;
	with public Text[] casing = [];
	with public Text name = "";
	with public Num flags = 0;

	Text[] -- getNeededImports() {
		return WakeType.getNeededImports();
	}
